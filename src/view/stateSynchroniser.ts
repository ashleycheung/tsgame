import { GameRenderState, GameRenderUpdate } from "../state/statefulObjectManager";


/**
 * Responsible for syncing the game state and the renderer
 * and applying linear interpolation
 */
export class StateSynchroniser {
  
  setState (state: GameRenderState): void {
  
  }
  
  update (update: GameRenderUpdate): void {
  
  }
  
}